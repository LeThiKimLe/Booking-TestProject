{
  "name": "test",
  "version": "1.0.0",
  "description": "test booking website",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chromedriver": "^120.0.1",
    "date-fns": "^3.2.0",
    "selenium-webdriver": "^4.16.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-html-reporters": "^3.1.7",
    "jest-junit": "^16.0.0",
    "mochawesome": "^7.1.3"
  },
  "jest": {
    "reporters": [
      "default",
      [
        "./node_modules/jest-html-reporters",
        {
          "pageTitle": "Test Report"
        }
      ]
    ]
  }
}
